# Code generated by docsgen; DO NOT EDIT.

openapi: 3.0.3
info:
  title: "{{ .Computed.service_name_kebab }} API"
  version: 1.0.0
servers:
  - url: "https://example.com/api/{{ .Computed.service_name_kebab }}"
    description: "prod"
  - url: "https://dev.example.com/api/{{ .Computed.service_name_kebab }}"
    description: "dev"
  - url: "http://127.0.0.1:{{ .Scaffold.http_port }}"
    description: "local"
{{ if or .Computed.enable_health_check_final .Computed.enable_user_example_final -}}
paths:
{{- if .Computed.enable_health_check_final }}
  /healthz:
    get:
      summary: "GET /healthz"
      operationId: getHealthz
      tags: ["Server"]
      responses:
        "200":
          description: "OK"
        "405":
          description: "Method Not Allowed"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: "Service Unavailable"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
{{- end }}
{{- if .Computed.enable_user_example_final }}
  /user/{id}:
    get:
      summary: "GET /user/{id}"
      operationId: getUserId
      tags: ["User"]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        "200":
          description: "OK"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          description: "invalid user id"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: "user not found"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: "Internal Server Error"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
{{- end }}
{{ else -}}
paths: {}
{{ end -}}
{{ if or .Computed.enable_health_check_final .Computed.enable_user_example_final -}}
components:
  schemas:
    ErrorResponse:
      type: object
      required: [msg]
      properties:
        msg:
          type: string
{{- if .Computed.enable_user_example_final }}
    User:
      type: object
      required: [createdAt, email, id, name, updatedAt]
      properties:
        createdAt:
          type: string
          format: date-time
        email:
          type: string
        id:
          type: integer
          format: int64
        name:
          type: string
        updatedAt:
          type: string
          format: date-time
{{- end }}
{{ end -}}
